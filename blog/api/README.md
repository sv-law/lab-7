# blog-api

This repository contains the RESTful API for accessing the blog database.

## mock-rest-server
```
cd mock-rest-server
docker compose up --build
```


## blog
```
cd blog
docker compose up --build
```

